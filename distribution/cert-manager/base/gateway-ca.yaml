apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-selfsigned-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: gateway-selfsigned-ca
  secretName: gateway-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
