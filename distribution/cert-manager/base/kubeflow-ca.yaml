apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubeflow-selfsigned-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: kubeflow-selfsigned-ca
  secretName: kubeflow-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
